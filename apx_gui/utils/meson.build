pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
utilsdir     = join_paths(pkgdatadir, 'apx_gui/utils')

sources = [
  '__init__.py',
  'gtk.py',
]

install_data(sources, install_dir: utilsdir )